<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <properties resource="druid.properties"/>

    <settings>
        <setting name="logImpl" value="STDOUT_LOGGING"/>
        <!-- 延迟加载的全局开关。当开启时，所有关联对象都会延迟加载。
              特定关联关系中可通过设置 fetchType 属性来覆盖该项的开关状态。-->
<!--        <setting name="lazyLoadingEnabled" value="true"/>-->
    </settings>

    <typeAliases>
        <typeAlias type="cn.edu.cque.mall.entity.Category" alias="category"/>
        <typeAlias type="cn.edu.cque.mall.entity.Product" alias="product"/>
        <typeAlias type="cn.edu.cque.mall.entity.User" alias="user"/>
        <typeAlias type="cn.edu.cque.mall.entity.OrderItem" alias="orderItem"/>
        <typeAlias type="cn.edu.cque.mall.entity.Order" alias="order"/>
    </typeAliases>

    <plugins>
        <plugin interceptor="com.github.pagehelper.PageHelper">
            <!-- 指定方言 -->
            <property name="dialect" value="mysql"/>
        </plugin>
    </plugins>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${driverClassName}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${username}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <package name="cn.edu.cque.mall.mapper"/>
    </mappers>
</configuration>